using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Samples
{
    public class SampleURDF : Sample
    {
        public override string Name { get { return "SamplePhysx_URDF"; } }
        public override string Thumb { get { return "Thumb_URDF.png"; } }
        public override string Desc { get { return "Sample for the URDF Robot"; } }

        public override void Start()
        {
            base.Start();
        }

        public override void Stop()
        {
            base.Stop();
        }

        public override void Update(float timeSinceLastFrame)
        {
            base.Update(timeSinceLastFrame);
        }
    }
}
